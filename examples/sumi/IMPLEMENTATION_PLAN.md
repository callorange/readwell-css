# Sumi (수묵 水墨) 테마 종합 개선 계획서 및 구현 체크리스트

> **"전통 서예의 필세(筆勢)와 흑백농담을 계승하고, 프로덕션 수준의 모던 웹 표준과 듀얼 렌더링 엔진을 구축한다."**  
> 본 문서는 `examples/sumi` 수묵 테마의 조형적 완성도를 보존하면서, 안정적인 점진적 향상(Progressive Enhancement)과 실시간 검증을 달성하기 위한 **4단계 실행 계획 및 체크리스트**입니다.

---

## 🏛️ 핵심 개발 원칙 (Core Principles)

1. **무손실 호환성 (Lossless Compatibility)**:
   - 기존의 모든 원본 에셋(PNG/JPG)은 1바이트의 유실 없이 그대로 보존합니다.
   - WebP 변환은 최신 브라우저를 위한 점진적 향상으로 적용하며, 미지원 브라우저를 위한 구형 포맷 폴백을 100% 보장합니다.
2. **듀얼 렌더링 & 실시간 검증 (Dual Engine & Live Evaluation)**:
   - SVG 벡터 방식이 서예 특유의 유기적인 '손맛'을 충분히 살릴 수 있는지 검증하기 위해, **기존 비트맵 방식과 신규 SVG 방식을 병렬로 탑재**합니다.
   - 독자 및 개발자가 화면에서 클릭 한 번으로 두 방식을 즉시 전환·비교할 수 있는 실시간 A/B 스위처를 제공합니다.
3. **Readwell 코어 철학 연동 (Readwell Philosophy)**:
   - E-Ink 디스플레이 친화성, 긴 글 가독성(Long-form Reading), 제로 디펜던시(Zero Dependency)의 가치를 수묵 미학과 융합합니다.
4. **단계별 격리 검증 (Step-by-Step Validation)**:
   - 한 번에 대규모 수정을 가하지 않고, 각 Phase가 완료될 때마다 브라우저에서 실제 작동 상태를 검증한 뒤 다음 단계로 전진합니다.

---

## 🗺️ 4단계 구현 로드맵 개요

```mermaid
flowchart LR
    subgraph Phase1 ["1단계: 듀얼 렌더링 & 접근성"]
        P1_1[에셋 3-Tier 폴백\n기존보존 + WebP] --> P1_2[SVG 스피너/트랙\n프로토타입]
        P1_2 --> P1_3[실시간 A/B 스위처\n& 비교존]
        P1_3 --> P1_4[WCAG 2.1 접근성\n포커스링/ARIA]
    end

    subgraph Phase2 ["2단계: 모듈화 & CJK 전통 조판"]
        P2_1[sumi.css 분리\n& @layer 정리] --> P2_2[인라인 style 청소\n& dvh 모바일 뷰포트]
        P2_2 --> P2_3[세로쓰기 RTL 보정\n& 휠 스크롤 매핑]
        P2_3 --> P2_4[text-emphasis 권점\n& CJK 약물 압축]
    end

    subgraph Phase3 ["3단계: 모던 웹 & 테마 확장"]
        P3_1[Scroll-driven 독서선\n& dialog TOC] --> P3_2[야간 사경 다크모드\n금묵/흑지]
        P3_2 --> P3_3[E-Ink 전자종이 모드\nupdate: slow]
        P3_3 --> P3_4[텍스트 훈염 드래그\n& LocalStorage]
    end

    subgraph Phase4 ["4단계: 기술 문서 컴포넌트 & DX"]
        P4_1[수묵 정규 표\n.rw-sumi-table] --> P4_2[문방사우 코드 하이라이트\n& 난외 배주 각주]
        P4_2 --> P4_3[@media print\n고서 목판본 인쇄]
        P4_3 --> P4_4[플레이그라운드\n& 코드 복사]
    end

    Phase1 --> Phase2 --> Phase3 --> Phase4
```

---

## 📋 단계별 상세 구현 체크리스트

### Phase 1: 듀얼 렌더링 기반 & 웹 접근성(WCAG 2.1) 안정화
> **목표**: 기존 에셋 100% 보존 상태에서 WebP 폴백 및 SVG 벡터 엔진 프로토타입을 구축하고, 실시간 비교 스위처 및 긴급 접근성 결함을 해결합니다.

- [x] **1-1. 에셋 3-Tier 보존 및 점진적 변환**
  - [x] 기존 원본 PNG/JPG 에셋 100% 무결점 보존 (`assets/` 내 원본 유지)
  - [x] `hanji-bg.jpg`(431KB)의 고압축 WebP(약 70KB) 생성
  - [x] 스피너 2대 스타일(`brush`, `enso`)의 알파 마스크 단일화 WebP/PNG 준비
  - [x] CSS `image-set()` 및 Cascading Fallback 선언으로 WebP 지원 브라우저 우선 로딩 및 구형 브라우저 완벽 폴백
  - [x] 인라인 아바타/인장 `<img>`에 `<picture><source type="image/webp">...` 표준 마크업 적용
- [x] **1-2. 차세대 SVG 벡터 엔진 프로토타입 구현**
  - [x] 비백호(飛白弧) 및 원상(圓相) 붓터치 스피너 SVG `<path>` 및 서예 회전 구현
  - [x] 수묵 프로그레스 & 슬라이더용 12px 벡터 스트로크 SVG 패스 트랙/채움 구현
  - [x] ~~0KB 인라인 SVG `<feTurbulence>` 한지 섬유결 절구 텍스처 타일~~ ➡️ **[폐기]** SVG 배경 폐기 결정: feTurbulence 노이즈의 가독성 저하 및 인위적인 줄무늬 결함으로 전면 폐기하고, 자연스러운 한지 질감은 고압축(72KB) WebP 비트맵으로 일원화
- [x] **1-3. 실시간 A/B 렌더링 엔진 스위처 및 1:1 비교존 구축**
  - [x] 우측 하단 플로팅 컨트롤러에 `🎨 렌더 엔진: [비트맵 (WebP/PNG)] ↔ [SVG 벡터]` 실시간 토글 버튼 추가
  - [x] `<body>`의 `data-rw-engine="bitmap" | "svg"` 데이터 속성에 따른 CSS 엔진 실시간 연동
  - [x] 쇼케이스 카드 내부에 **[기존 비트맵 방식] vs [신규 SVG 방식] 1:1 Side-by-Side 비교 영역** 구성
- [x] **1-4. 웹 접근성(WCAG 2.1) 결함 즉시 해결**
  - [x] 슬라이더의 `outline: none !important` 제거 및 키보드 초점 시 붉은 인주 훈염 링(`:focus-visible`) 표시
  - [x] `<input type="range">`에 시맨틱 `aria-label="수묵 붓점 조절기"` 추가
  - [x] 프로그레스 바에 `role="progressbar"`, `aria-valuenow`, `aria-valuemin`, `aria-valuemax` 명시
  - [x] 4분면 전각 직인 컴포넌트에 `role="img"`, `aria-label="독서정본 직인"` 부여로 글자 분절 낭독 차단
  - [x] 쇼케이스 전시용 스피너들에 `aria-hidden="true"`를 부여하여 스크린 리더 소음 제거
  - [x] 12px 슬림 슬라이더에 가상 요소(`::after`)를 적용해 최소 **44px × 44px 터치 조작 타겟** 확보
- [x] **[Phase 1 검증]**: 브라우저에서 비트맵/SVG 토글 확인, 키보드 Tab 키 탐색 및 스크린 리더 음성 출력 검증

---

### Phase 2: 아키텍처 모듈화 & 동양 전통 조판(CJK) 정밀화
> **목표**: 2,000줄의 인라인 모놀리식 CSS를 독립 라이브러리로 분리하고, 전통 서첩 조판의 미학을 완성합니다.

- [ ] **2-1. 독립 모듈 추출 및 Cascade Layers 정리**
  - [ ] `index.html` 내 인라인 CSS를 `examples/sumi/sumi.css`로 추출 분리
  - [ ] Readwell 코어 CSS 레이어 체계(`@layer themes.sumi`, `@layer components.sumi`)와 연동
  - [ ] 불필요하게 남발된 CSS `!important` 선언 정리
- [ ] **2-2. 코드 위생 및 모바일 뷰포트 개선**
  - [ ] HTML 본문 속 수십 개의 인라인 하드코딩 `style="..."` 속성을 시맨틱 CSS 클래스로 리팩토링
  - [ ] 캔버스 및 레이아웃의 `100vh`/`100vw`를 Dynamic Viewport 단위(`100dvh`/`100dvw`)로 전환하여 모바일 주소창 출렁임 버그 해결
  - [ ] 장문 아티클 하단 오프스크린 섹션에 `content-visibility: auto` 렌더링 최적화 적용
- [ ] **2-3. 고서 전통 세로쓰기(縱書) UX 결함 완전 해결**
  - [ ] `.rw-sumi-vertical-wrap`의 가로 스크롤 초기 위치를 고서 정통 읽기 시작점인 **우측 끝(RTL 시작점)**으로 자동 정렬
  - [ ] 마우스 휠 이벤트(`deltaY`)를 가로 스크롤(`scrollLeft`)로 매핑하여 부드러운 병풍/서첩 넘김 인터랙션 지원
- [ ] **2-4. 전통 권점(圈點) 및 CJK 타이포그래피 고도화**
  - [ ] `text-emphasis: filled circle var(--rw-sumi-seal)`를 활용한 전통 붉은 주사(朱砂) 권점 강조 클래스(`.rw-sumi-emphasis`) 구현
  - [ ] 최신 CSS `text-spacing-trim: normal` 적용으로 CJK 문장 부호(괄호/쉼표/마침표) 연속 여백 자동 압축
  - [ ] `line-break: strict; word-break: keep-all;`로 행두 금칙 문자 엄격 적용
  - [ ] 담묵 소자(小字) 루비 주석 스타일링(`<ruby>`, `<rt>`) 지원
- [ ] **[Phase 2 검증]**: 분리된 `sumi.css` 로딩 확인, 세로쓰기 가로 휠 스크롤 및 RTL 시작 위치 확인, 권점 강조 렌더링 점검

---

### Phase 3: 모던 웹 표준 & 테마 확장 (다크 모드 & E-Ink)
> **목표**: 현대 브라우저 표준 API를 탑재하고, 야간 사경 다크 모드와 전자종이(E-Ink) 전용 모드를 완성합니다.

- [ ] **3-1. 모던 웹 표준 인터랙션 전환**
  - [ ] 스크롤 독서 게이지에 CSS Scroll-Driven Animations(`animation-timeline: scroll()`) 적용 (미지원 브라우저는 기존 JS 폴백)
  - [ ] 커스텀 모바일 TOC를 HTML5 시맨틱 `<dialog>` 및 Popover API로 전환 (Top Layer, `::backdrop`, ESC 키 자동 닫기)
  - [ ] 모바일 TOC 바텀시트에 터치 스와이프 닫기(Swipe-to-dismiss) 제스처 추가
  - [ ] 테마/서체 전환 시 `document.startViewTransition()` 화선지 먹물 번짐 트랜지션 연출
- [ ] **3-2. 컨셉 확장: 야간 사경(寫經) / 현묵(玄墨) 다크 모드**
  - [ ] 칠흑 농묵 흑지(黑紙, `#121316`), 백묵(白墨, `#d6dadf`) 본문, 고서 사경의 금묵(金墨, `#d4af37`) 헤드라인 컬러 토큰 정의
  - [ ] `[data-rw-theme="sumi-dark"]` 및 `@media (prefers-color-scheme: dark)` 토글 지원
  - [ ] 다크 모드 전용 흑지 질감 캔버스 및 주사 인장 콘트라스트 보정
- [ ] **3-3. Readwell 정체성 연계: E-Ink (전자종이) 모드**
  - [ ] 표준 미디어 쿼리 `@media (update: slow)` 자동 감지 지원
  - [ ] 전자종이 모드(`[data-rw-mode="eink"]`): 한지 비트맵 결 및 그라디언트 비활성화 (순수 단색 화선지 모드)
  - [ ] 무한 회전 스피너 대신 E-Ink 특화 정적 인디케이터/단계별 스텝 펄스로 전환
  - [ ] 1-bit / 4-bit 단색 서예 고대비 조판 최적화
- [ ] **3-4. 수묵 마이크로 인터랙션 & 설정 영속화**
  - [ ] 본문 텍스트 드래그 선택 시 주사(朱砂) 훈염 하이라이트(`::selection`) 적용
  - [ ] 플로팅 컨트롤러에 장문 독서용 활자 크기 조절기(A- / A+) 추가
  - [ ] 서체, 한지 결, 다크 모드, 렌더 엔진 설정을 `localStorage`에 영구 저장 및 재방문 시 자동 복원
- [ ] **[Phase 3 검증]**: 다크 모드 및 E-Ink 모드 전환 테스트, DevTools 센서 탭에서 `update: slow` 감지 확인, 페이지 새로고침 시 설정 유지 확인

---

### Phase 4: 기술 문서 컴포넌트 생태계 & DX 마감
> **목표**: 기술 문서 특화 컴포넌트를 완비하고, 인쇄 최적화 및 개발자 경험(DX)을 극대화합니다.

- [ ] **4-1. 기술 문서 조판 컴포넌트 추가**
  - [ ] 상단 농묵 굵은 획(2px), 지선(紙線) 엷은 보더, 담묵 호버 효과가 적용된 수묵 정규 표(`.rw-sumi-table`) 컴포넌트 모듈화
  - [ ] 주사(키워드), 송화(문자열), 농묵(함수), 담묵(주석)으로 구성된 **문방사우 코드 신택스 하이라이팅** 테마 적용
  - [ ] 본문 인장 번호(`①`) 클릭 시 우측 메타 레일에서 주석이 강조되는 **동양 고서 난외 배주(Marginalia) 주석 시스템** 구축
- [ ] **4-2. 인쇄 미디어 쿼리 (`@media print`) 고서 목판본 조판**
  - [ ] 배경 한지 텍스처, 플로팅 버튼, 스피너, 사이드바 메뉴 자동 숨김 처리
  - [ ] 고서 목판본 스타일의 흑백 서예 레이아웃으로 A4/PDF 출력 최적화
- [ ] **4-3. 쇼케이스 DX 및 개발자 인터랙션 강화**
  - [ ] 직인, 슬라이더, 스피너, 콜아웃 마크업을 즉시 복사할 수 있는 **원클릭 코드 복사 (Copy Snippet)** 인터랙션 추가
  - [ ] 서예 동세(7종), 스피너 크기, 먹색(농묵/주사/담묵/금묵)을 실시간으로 조작하는 **인터랙티브 플레이그라운드 패널** 완성
  - [ ] 전각 두인(頭印) 형태 다양화: 타원형 외에 **표주박형(호로인 葫蘆印)**, **자연석형(자연인 自然印)** CSS 프리셋 추가
- [ ] **[Phase 4 검증]**: 인쇄 미리보기(Ctrl+P) 레이아웃 확인, 코드 복사 클립보드 동작 확인, 플레이그라운드 실시간 조작 확인

---

## ⚖️ 듀얼 렌더링 (비트맵 vs SVG) 평가 기준표 (Evaluation Matrix)

독자 및 개발자가 두 방식을 직접 테스트할 때 객관적으로 판단할 수 있는 4대 평가 기준입니다:

| 평가 지표 | 비트맵 방식 (WebP + PNG/JPG) | SVG 벡터 방식 | 승자 판정 기준 |
| :--- | :--- | :--- | :--- |
| **전송량 (Transfer Size)** | WebP 압축 시 약 150~250KB | 인라인 코드 약 5~15KB | **SVG 압승** (90% 이상 절감) |
| **시각적 손맛 (Artistic Fidelity)** | 서예가의 실제 붓질, 비백, 화선지 번짐 손맛 100% 보존 | 수학적 곡선으로 인해 미세하게 인위적일 가능성 있음 | **1:1 육안 비교로 판정** |
| **디스플레이 확장성 (High DPI)** | 200%, 300% 모바일 레티나 화면에서 미세 흐림 가능 | 어떤 해상도(4K/8K)나 확대 비율에서도 칼같은 선명도 유지 | **SVG 압승** |
| **렌더링 부하 (Scroll FPS)** | 브라우저 디코딩 후 비트맵 단순 페인팅 (GPU 부하 극소) | 복잡한 SVG 필터나 실시간 노이즈 연산 시 모바일 GPU 부하 가능 | **저사양 기기 60fps 유지 여부로 판정** |
| **테마 확장성 (Color Tuning)** | 색상별(농묵, 주사, 담묵, 금묵) 별도 에셋 또는 마스크 필요 | CSS `fill`, `stroke` 변수 하나로 임의의 안료 색상 즉시 대응 | **SVG 압승** |

---

## 🛡️ 조형 무결성 거버넌스 (Design Integrity Rules)

구현 중 절대 훼손되지 않아야 할 서예 미학의 4대 기본 수칙입니다:

1. **Anti-Bulking (슬림한 선율 보존)**: 프로그레스 바와 슬라이더의 높이는 `12px`(획 두께 6~8px)를 절대 기준으로 유지합니다.
2. **기필(起筆)과 수필(收筆)의 호응**: 시작점의 유선형 둥근 붓머리와 끝단의 날렵한 테이퍼링(소멸 궤적)을 보존합니다.
3. **트랙과 채움의 1:1 실루엣 합치**: 담묵 트랙과 농묵 채움은 동일한 수묵 궤적 마스크를 공유해야 합니다.
4. **여백지미 (余白之美)**: 인위적인 기하학적 박스나 네온 테두리를 배제하고, 화선지의 호흡과 먹의 농담으로 문맥을 표현합니다.

---

*문서 생성일: 2026-09-06*  
*관리 대상: `d:/Projects/Private/readwell-css/examples/sumi/`*
